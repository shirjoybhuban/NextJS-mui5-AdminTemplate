import { Typography } from '@mui/material';
import Layout from 'components/layout/Layout';
import React from 'react';

const Agents = () => {
  return (
    <Layout>
      <Typography sx={{ p: 5 }} variant="h3" color="initial">
        Agents
      </Typography>
    </Layout>
  );
};

export default Agents;
